language: python
dist: trusty
sudo: true
before_install:
  - sudo apt-get install -y vlc
python:
  - "3.4"
  - "3.5"
  - "3.6"
  - "3.7"
install:
  - pip install -r requirements-dev.txt
script: pytest
